<!DOCTYPE html>
<html>
<head>
  <title>The Error Logs</title>
  <link href='https://fonts.googleapis.com/css?family=Noto+Sans:400,700|Noto+Serif|' rel='stylesheet' type='text/css'>
  
  <%= stylesheet_link_tag  'application', media: 'all', 'data-turbolinks-track' => true %>
  
  
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  
  
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body <%= yield(:body_attributes) %>>
  

<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container" id="navbar-contain">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%if user_signed_in?%>
        <%= link_to root_path, id: 'brand' do %>
          <%= image_tag "black.png", class:'navbar-brand img-responsive' %>
        <%end%>
      <% else %>
        <%= link_to root_path do %>
          <%= image_tag "black.png", class:'navbar-brand img-responsive' %>
        <%end%>
      <%end%>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if user_signed_in? %>
        <ul class ="nav navbar-nav navbar-left left-border updates">
          <li><%= link_to "<span class='glyphicon glyphicon-bell'></span>&nbsp; Updates".html_safe, friends_posts_path%></li>
        </ul>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to '<span class="glyphicon glyphicon-user"></span>&nbsp; Profile'.html_safe, user_path(current_user.id),  data: { no_turbolink: true }, class: "left-border" %></li>
          <li><%= link_to '<span class="glyphicon glyphicon-pencil"></span>&nbsp; New Post'.html_safe, new_post_path%></li>
          <li><%= link_to('<span class="glyphicon glyphicon-log-out"></span>&nbsp; Logout'.html_safe, destroy_user_session_path,:method => :delete) %></li>
      </ul>
        <% else %>
          <li><%= link_to '<span class="glyphicon glyphicon-log-in"></span>&nbsp; Login'.html_safe, new_user_session_path, class: "left-border", data: { no_turbolink: true } %></li>
          <li><%= link_to '<span class="glyphicon glyphicon glyphicon-ok-sign"></span>&nbsp; Signup'.html_safe, new_user_registration_path %></li>
      </ul>
        <% end %>
    
    
    </div>
  </div> <!--container-->
</nav>
<% if flash[:notice] %>
    <div class="notice"><%= flash[:notice] %></div>
   <% elsif flash[:alert]%>
     <div class="alert"><%= flash[:alert] %></div>
<% end %>
<%= yield %>

</body>
</html>
