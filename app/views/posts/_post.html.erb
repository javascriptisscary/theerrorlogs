
<% if can? :read, post %>
   <h1><strong><%= link_to post.title, post_path(post.id) %></strong></h1>
   <%= post.content.html_safe %>
<% end %>
<% if can?:edit, post %>
   <%= link_to "Edit", edit_post_path(post) %>
<% end %>
