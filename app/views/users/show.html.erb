<div class="container">
  <h1 class="text-center"><%=@user.email%> Profile Page</h1>
  
  <p>Will have a profile here, perhaps latest entry.</p>
<%=image_tag @user.avatar(:medium) %>


<%=link_to 'blog entries', posts_path(user_id: @user.id) %>
  
  
</div>
